services:
  idp-service:
    environment:
      # Override explicit defaults here; any extra keys from deploy .env now flow through automatically.
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      # Expose additional ports if needed
      - "5000:5000"

  mssql-service:
    environment:
      - MSSQL_SA_PASSWORD=NewStrong!Passw0rd
